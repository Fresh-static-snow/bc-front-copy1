import { css } from '@emotion/react';
import styled from '@emotion/styled';

export const DateAdditional = styled('div')(
  () => css`
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 0 0 6px;
  `,
);
