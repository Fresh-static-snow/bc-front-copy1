export type FieldErrorMessageProps = {
  errorMessage: string;
  /**
   * @default 'absolute'
   */
  position?: 'relative' | 'absolute';
};

export type StyledRootProps = {
  $position: 'relative' | 'absolute';
};
