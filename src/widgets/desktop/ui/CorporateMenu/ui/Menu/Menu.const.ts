import { UpdateCorporate } from '@/features/corporate';
import { FormTemplates } from '@/shared/types/values.types';

export const editFormTemplates: FormTemplates = {
  corporate: UpdateCorporate,
};
